Drop provider logos here as SVG files named by provider id.

Examples:
- `codex.svg`
- `openrouter.svg`
- `claude.svg`

The app resolves logos from:
- `/provider-logos/<providerId>.svg`

If a logo is missing, the UI falls back to a single-letter avatar.
